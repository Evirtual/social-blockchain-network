name = "social-posts-pinata"
main = "src/index.ts"
compatibility_date = "2024-12-01"

# Set via: wrangler secret put PINATA_JWT

[vars]
# CORS allowlist:
# - "*" to allow any origin (simple demos)
# - or a comma-separated list like "https://yourdomain.com,http://localhost:5173"
ALLOW_ORIGINS = "https://social.edgarasneverdauskas.com,https://evirtual.github.io,http://localhost:5173"
